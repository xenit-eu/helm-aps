apiVersion: v1
kind: Service
metadata:
  name: alfresco-activity-admin-service
  namespace: {{ .Release.Namespace | quote }}
  annotations:
    {{- if .Values.activityAdmin.serviceAnnotations }}
    {{ toYaml .Values.activityAdmin.serviceAnnotations | nindent 4 }}
    {{- end }}
spec:
  selector:
    app: alfresco-activity-admin
  ports:
      - name: 'alfresco-activity-admin'
        protocol: TCP
        port: 30000
        targetPort: 8080
      - name: 'alfresco-activity-admin-ssl'
        protocol: TCP
        port: 8443
        targetPort: 8443
  {{- if .Values.general.serviceType }}
  type: {{ .Values.general.serviceType }}
  {{- end }}